import { Box, Grid } from "@mui/material"

const FilterBox = () => {
    return (
        <Box sx={{ paddingTop: 10 }}>
            <div>Filter</div>
        </Box>
    );
};

export default FilterBox;