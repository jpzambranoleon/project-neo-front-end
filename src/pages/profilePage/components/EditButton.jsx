import { Box, Button } from "@mui/material";

const EditButton = () => {
    return (
        <Box sx={{ mt: 8 }}>
            <Button variant="outlined">
                Edit Profile
            </Button>
        </Box>
    );
};

export default EditButton;