import { Box } from "@mui/material"
import User from "./User";

const Leftbar = () => {
    return (
        <Box sx={{ paddingTop: 10 }}>
            <User />
        </Box>
    );
};

export default Leftbar;